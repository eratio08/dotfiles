---
description: Ask the user questions with a free-text answer option
---
Use the `question` tool for every question you ask the user.
Never ask questions in a normal assistant message.
Set `custom: true` for every question so the user always has a free-text answer option.
Do not add a separate `Other` option because the `question` tool provides the free-text option.
